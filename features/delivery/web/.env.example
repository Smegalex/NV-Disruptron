# Set to false to hide all demo UI (preview buttons, auto notification simulation)
VITE_ENABLE_DEMO=true

# Speech-to-text: api (local via disruptron-api), browser (Web Speech API), or auto
VITE_STT_MODE=auto
